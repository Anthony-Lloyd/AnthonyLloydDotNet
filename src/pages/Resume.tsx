import React, { useState, useEffect } from 'react';
  import { marked } from 'marked';

import '../index.css';
import './styles/resume.css';

const Resume: React.FC = () => {
  return (
    <div className="x">
      {/* ... list of services using cards or simple list groups ... */}
    </div>
  );
}

export default Resume;
